# Persistent chrome shell (historical)

> **Archive.** Chrome is React; content is iframe or local panels.

Earlier work tried `shell.DualHost` (chrome webview_go + content go-webview2). Superseded by Wails + React + `BrowserView`.
